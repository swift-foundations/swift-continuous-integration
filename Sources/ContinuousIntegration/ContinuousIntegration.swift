// Nest.Name namespace shell. `ContinuousIntegration` owns the
// vendor-neutral subject/event/plan/aggregate semantics extracted from
// the Institute CI `CI.Contract` target (TX-APP2A) and preserves the
// required check context `ci / matrix / ci-ok`.
public enum ContinuousIntegration {}
